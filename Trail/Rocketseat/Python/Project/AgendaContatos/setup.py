from setuptools import setup, find_packages

setup(
    name='agendacontatos',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Dependências do seu projeto, se houver
    ],
)